# OpenCode Configuration — AI Developer Skill OS v10.2.0
# Auto-generated by bin/install.js --ide=opencode

rules:
  - path: AGENTS.md
    always: true
  - path: .agents/rules/global.md
    always: true
  - path: .agents/rules/coding.md
    always: true

skills:
  directory: .agents/skills
  registry: .agents/registry
  platforms: [antigravity, claude, opencode]

context:
  max_tokens: 128000
  include:
    - .agents/AGENTS.md
    - .agents/rules/
    - .agents/skills/
    - .agents/workflows/

execution:
  confidence_model: true
  exit_codes: [SUCCESS, PARTIAL, BLOCKED, FAILED]
  evidence_format: true
  compliance_check: true

validation:
  command: "node tooling/validate-skills.js && node tooling/validate-graph.js"
  on_save: true
