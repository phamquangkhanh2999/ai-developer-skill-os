case:
  id: sec-001
input:
  "Kiểm tra xem thư viện có lỗi bảo mật không"
expected:
  primary_skill:
    qk-security-audit
  forbidden:
    - qk-validation-gate
  confidence:
    min: 0.8
conflict_policy:
  severity: warning
  action: continue_with_notice
  expected_message: "Conflict detected in sec-001"
