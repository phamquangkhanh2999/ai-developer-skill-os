{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://antigravity-kernel/schemas/generated/intent_record.schema.json",
  "title": "IntentRecord Schema Validator",
  "description": "DO NOT EDIT MANUALLY. Generated strictly from Canonical Semantic Model and Hiến pháp ABI. Any direct edits violate Phase 0 Contract Governance.",
  "type": "object",
  "required": [
    "intent_id",
    "creation_timestamp",
    "lifecycle_state",
    "primary_goal",
    "scope_boundaries",
    "domain_constraints",
    "contract_version"
  ],
  "additionalProperties": false,
  "properties": {
    "intent_id": {
      "type": "string",
      "format": "uuid",
      "description": "Immutable identity coordinate for the IntentRecord."
    },
    "creation_timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "UTC chronological marker recording intent synthesis time."
    },
    "lifecycle_state": {
      "type": "string",
      "enum": ["CREATED", "PUBLISHED", "FROZEN"],
      "description": "Finite lifecycle operating state governed strictly by state_machine.md."
    },
    "primary_goal": {
      "type": "string",
      "minLength": 1,
      "description": "Concrete statement of the desired business or technical outcome."
    },
    "scope_boundaries": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string" },
      "description": "Explicit architectural zones, file paths, or namespace limits for allowed actions."
    },
    "domain_constraints": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Security and operational rules that executing capabilities must not violate."
    },
    "confidence_score": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "Heuristic certainty metric regarding intent disambiguation."
    },
    "context_references": {
      "type": "array",
      "items": { "type": "string", "format": "uuid" },
      "description": "Optional historic execution IDs or conversational pointers."
    },
    "contract_version": {
      "type": "string",
      "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)$",
      "description": "Active Kernel ABI semantic contract version."
    }
  }
}
