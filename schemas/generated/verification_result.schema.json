{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://antigravity-kernel/schemas/generated/verification_result.schema.json",
  "title": "VerificationResult Schema Validator",
  "description": "DO NOT EDIT MANUALLY. Generated strictly from Canonical Semantic Model and Hiến pháp ABI. Any direct edits violate Phase 0 Contract Governance.",
  "type": "object",
  "required": [
    "verification_id",
    "target_result_id",
    "verifier_engine_id",
    "test_suite_category",
    "verdict",
    "contract_version"
  ],
  "additionalProperties": false,
  "properties": {
    "verification_id": {
      "type": "string",
      "format": "uuid",
      "description": "Immutable unique identity coordinate for this specific verification evaluation report."
    },
    "target_result_id": {
      "type": "string",
      "format": "uuid",
      "description": "Causality pointer referencing target operational result or manifest under test."
    },
    "verifier_engine_id": {
      "type": "string",
      "minLength": 2,
      "description": "Signature representing independent QA pipeline or Conformance harness."
    },
    "test_suite_category": {
      "type": "string",
      "enum": ["SCHEMA_L1", "BEHAVIORAL_L2", "BUDGET_L3", "INTEGRATION_SUITE", "SECURITY_AUDIT"],
      "description": "Categorization of applied verification testing regime."
    },
    "verdict": {
      "type": "string",
      "enum": ["PASS", "FAIL", "INCONCLUSIVE", "BYPASSED"],
      "description": "Final verification quality evaluation judgment."
    },
    "failed_assertions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["assertion_code", "error_description"],
        "properties": {
          "assertion_code": { "type": "string" },
          "error_description": { "type": "string" },
          "location_path": { "type": "string" }
        },
        "additionalProperties": false
      },
      "description": "Explicit enumeration of syntax breaches, invariant transgressions, or assertion errors encountered."
    },
    "diagnostic_artifacts": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^sha256:[a-f0-9]{64}$"
      },
      "description": "Cryptographic SHA-256 hash digests representing external diagnostic logs or trace files."
    },
    "contract_version": {
      "type": "string",
      "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)$",
      "description": "Active Kernel ABI semantic contract version."
    }
  }
}
