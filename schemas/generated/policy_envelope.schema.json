{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://antigravity-kernel/schemas/generated/policy_envelope.schema.json",
  "title": "PolicyEnvelope Schema Validator",
  "description": "DO NOT EDIT MANUALLY. Generated strictly from Canonical Semantic Model and Hiến pháp ABI. Any direct edits violate Phase 0 Contract Governance.",
  "type": "object",
  "required": [
    "policy_id",
    "cost_tier",
    "tool_call_budget",
    "reasoning_burn_ceiling",
    "safety_guardrails",
    "escalation_matrix",
    "contract_version"
  ],
  "additionalProperties": false,
  "properties": {
    "policy_id": {
      "type": "string",
      "format": "uuid",
      "description": "Immutable identity coordinate for causality tracking."
    },
    "cost_tier": {
      "type": "string",
      "enum": ["INSTANT", "TARGETED", "DEEP"],
      "description": "Assigned operational resource cost class tier."
    },
    "tool_call_budget": {
      "type": "integer",
      "minimum": 0,
      "description": "Maximum authorized count of tool invocations."
    },
    "reasoning_burn_ceiling": {
      "type": "integer",
      "minimum": 0,
      "description": "Numerical tolerance limit bounding computational reasoning expenditures."
    },
    "safety_guardrails": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Security constraints such as read-only rules or directory isolations."
    },
    "escalation_matrix": {
      "type": "object",
      "required": ["allow_escalation", "mandatory_user_confirmation"],
      "properties": {
        "allow_escalation": { "type": "boolean" },
        "mandatory_user_confirmation": { "type": "boolean" },
        "fallback_tier": { "type": "string", "enum": ["INSTANT", "TARGETED", "DEEP"] }
      },
      "additionalProperties": true,
      "description": "Declares valid escalation triggers and fallback behaviors."
    },
    "timeout_duration": {
      "type": "integer",
      "minimum": 1,
      "description": "Maximum wall-clock seconds authorized for capability execution."
    },
    "contract_version": {
      "type": "string",
      "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)$",
      "description": "Active Kernel ABI semantic contract version."
    }
  }
}
