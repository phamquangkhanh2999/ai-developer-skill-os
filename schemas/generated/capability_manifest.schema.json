{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://antigravity-kernel/schemas/generated/capability_manifest.schema.json",
  "title": "CapabilityManifest Schema Validator",
  "description": "DO NOT EDIT MANUALLY. Generated strictly from Canonical Semantic Model and Hiến pháp ABI. Any direct edits violate Phase 0 Contract Governance.",
  "type": "object",
  "required": [
    "capability_id",
    "contract_version",
    "supported_scopes",
    "required_guarantees",
    "compatibility_bounds",
    "cost_profile",
    "trust_level"
  ],
  "additionalProperties": false,
  "properties": {
    "capability_id": {
      "type": "string",
      "minLength": 3,
      "description": "Namespaced identifier coordinates representing specialist driver."
    },
    "contract_version": {
      "type": "string",
      "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)$",
      "description": "Semantic version number of this driver manifest release."
    },
    "supported_scopes": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string" },
      "description": "Explicit enumeration of business goal problem domains and file types supported."
    },
    "required_guarantees": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Mandatory environmental capabilities or sandbox permissions required from kernel."
    },
    "compatibility_bounds": {
      "type": "object",
      "required": ["minimum_kernel_abi", "maximum_kernel_abi", "required_policy_features"],
      "properties": {
        "minimum_kernel_abi": { "type": "string", "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)$" },
        "maximum_kernel_abi": { "type": "string" },
        "required_policy_features": { "type": "array", "items": { "type": "string" } }
      },
      "additionalProperties": false,
      "description": "Bidirectional Kernel vs Driver semantic interoperability boundaries."
    },
    "cost_profile": {
      "type": "object",
      "required": ["minimum_policy_tier", "maximum_policy_tier", "supports_auto_escalation"],
      "properties": {
        "minimum_policy_tier": { "type": "string", "enum": ["INSTANT", "TARGETED", "DEEP"] },
        "maximum_policy_tier": { "type": "string", "enum": ["INSTANT", "TARGETED", "DEEP"] },
        "supports_auto_escalation": { "type": "boolean" }
      },
      "additionalProperties": false,
      "description": "Resource consumption expectation limits and tier boundaries."
    },
    "trust_level": {
      "type": "object",
      "required": ["classification", "verification_badges"],
      "properties": {
        "classification": { "type": "string", "enum": ["experimental", "sandbox", "verified", "core"] },
        "verification_badges": { "type": "array", "items": { "type": "string" } }
      },
      "additionalProperties": false,
      "description": "Security classification and conformance verification testing credentials."
    },
    "limitations_disclosure": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Optional admissions of unsupported edge cases or architecture limits."
    }
  }
}
